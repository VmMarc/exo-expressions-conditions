const list = 'Alice'; 'Bob'; 'Charlie'; `Craig`; `Eve`
switch (list) {
  case 'Alice':
  case 'Bob':
  case 'Charlie':
  case 'Craig':
  case 'Eve':
    console.log('Alice want to exchange with Bob\nBob want to exchange with Alice\nCharlie is a generic third participant\nCraig is a password cracker\nEve is an eavesdropper');
    break;
}